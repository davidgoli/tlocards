- content_for :head do
  = render 'shared/common_header'
%p Redeemed codes for
%h1= "#{@download.artist} - #{@download.title}"

%table
  %thead
    %td Code
    %td Email
    %td Date Redeemed
    %td User IP
  %tbody
    - redeemed_codes(@codes) do |code|
      %tr
        %td
          = code.code
        %td
          = code.email
        %td
          = code.redeemed_date
        %td
          = code.user_ip
